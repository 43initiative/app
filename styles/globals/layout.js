import {StyleSheet} from "react-native";

export  default StyleSheet.create({
    vertical : {
       display:'flex',
        flexDirection:'column'
    },
    horizontal : {
        display: 'flex',
        flexDirection: 'row',
    }
})
